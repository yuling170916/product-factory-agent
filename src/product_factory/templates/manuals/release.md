# 上线手册（固定资产）

上线是受控操作，不是代码生成阶段的自然延伸。

- 任何远程写入、生产权限申请、域名或密钥变更必须由人批准。
- 上线前保存版本号、commit、构建产物、配置差异和数据库迁移计划。
- 必须定义健康检查、核心冒烟用例、监控指标、告警负责人和回滚条件。
- 密钥只从环境或密钥管理服务读取，禁止进入仓库、提示词或日志。
- 数据库迁移必须说明向前兼容、备份、执行顺序与回滚限制。
- 上线后验证应针对真实目标 URL，并区分“未验证”“失败”“通过”。
- 验证失败时停止扩大流量，保留证据并按预案回滚或修复。
