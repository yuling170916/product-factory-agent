# 阶段 Spec：集成版本开发验证

验证完整用户旅程、前后端契约、权限边界、异常与空状态、响应式表现、构建和回归测试。报告使用“通过/失败/阻塞/未执行”四态，附证据与复现步骤。失败项不得被总结成“整体通过”。

先复用已有 integration 浏览器证据，再独立重跑 `npm ci`、lint、typecheck、test 和 build。若必须启动本地 preview，必须使用可控后台进程，记录 PID，在检查后终止，并保证失败路径也清理；禁止把长运行服务器留在前台等待。不要部署、访问生产环境或修改流程状态。
